# TODO

## 约定

- 内部所有角度计算统一使用弧度；调试界面中的角度统一转换为度显示。
- E0 在首次获得装甲板观测时初始化。目标丢失时重置 E0；暂不处理不同目标之间的切换。
- 槽位匹配不设置拒绝阈值，始终选择总代价最小的槽位。
- 双板同现时，只将重投影误差较小的装甲板作为本帧 EKF 观测；另一块仍参与槽位匹配。
- 调试页的参数修改仅实时作用于当前进程，不写回本地配置文件。
- 灯条观测升维属于 `advance` 阶段，当前不实现。

## 第一阶段：装甲板 yaw 与槽位匹配

- [x] 梳理 IPPE 输出、云台角度、装甲板 outward yaw 的坐标系和转换关系，确保内部角度均为弧度。
- [x] 在当前云台 yaw 的 `+/-70 deg` 候选范围内，使用 outward pitch `15 deg` 先验计算重投影误差，并选择误差最小的观测 yaw。
- [x] 实现 E0 初始化：首个观测装甲板作为 E0；若首帧同时观测到多个装甲板，选择置信度最高者作为 E0。
- [x] 基于 E0 生成其余三个槽位，并在代码中注明暂未实现跨目标切换处理。
- [x] 实现槽位代价函数：装甲板与槽位在 `x-y` 平面距离项，加上装甲板 yaw 与槽位 yaw 的角度差项。
- [x] 为距离项和角度项定义可调权重，使不同量纲的代价可比较；每帧选择总代价最小的槽位。
- [x] 接入目标丢失处理：目标丢失时清除 E0 和全部槽位状态，下一次有效观测重新初始化。
- [x] 单板观测时直接使用该板 yaw；双板观测时依据重投影误差选择 EKF 输入板，并保留两块板的槽位匹配结果。

验收：目标以匀速自转时，槽位持续匹配，不出现仅识别一块板后槽位无法随目标转动的情况。

## 第二阶段：EKF

- [x] 保持现有过程模型和基础过程噪声设置不变。
- [x] 保持观测量为 `x, y, z, yaw`，并将 `Rx`、`Ry`、`Rz` 拆分为独立、可实时调节的观测方差。
- [x] 参考 `E:\DaedalusSimulator\sp_vision_25` 中的 `Ryaw` 噪声设置，实现与装甲板正对相机角度相关的 `Rtheta`。
- [x] 在预测 yaw 参与任何差分、角速度计算或状态更新前进行角度环绕；禁止累积为数千度的未环绕角度。
- [x] 明确并测试 yaw 创新量使用最短环绕角差。

验收：目标以匀速自转时，未经 EKF 的单板观测 yaw 曲线连续，并接近匀速变化，不出现周期性减速或跳变。

## 第三阶段：火控

- [x] 阅读并定位 `sp_vision_25` 中火控迭代收敛逻辑与条件判断的实现位置。
- [x] 将本项目火控迭代、收敛条件和退出/失效条件对齐该参考实现，并保持本项目已有接口约定。
- [x] 覆盖正常收敛、无法收敛和目标状态失效的行为测试。

## 第四阶段：调试与 UI

- [x] 绘制持续跟踪 E0 装甲板的 outward yaw 连续曲线，界面中以度显示。
- [x] 调整平面投影图：相机原点固定，仅绘制目标车体相对相机的运动轨迹/位置。
- [x] 以矩阵形式显示 `Q`、`R`、`P0`；矩阵元素使用具有物理含义的参数或表达式标注。
- [x] 在矩阵显示下方按算法流程从上到下提供实时调参控件，修改不持久化到本地文件。
- [x] 调细 UI 中装甲板框线，并缩小角点标记。

验收：页面的参数改动立即改变当前算法行为；重启进程后恢复原有配置值。

## 后续阶段：advance

- [ ] 使用传统视觉方法检测已识别装甲板框邻近的候选灯条。
- [ ] 仅针对未识别装甲板相邻的两块板，按车体模型和最小灯条重投影方差完成匹配。
- [ ] 将成功匹配的灯条以黄色标注在 UI 中。
- [ ] 复查并统一所有 UI 检测框的细线样式。
